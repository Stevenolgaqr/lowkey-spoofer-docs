# (AMD) RAID Reinstallation + Disk Bypass

للأنظمة التي تحتوي على معالجات AMD ودعم RAID.

---

## 🎯 هل تحتاج هذه الطريقة؟

✅ لديك معالج AMD (Ryzen)  
✅ لديك قرصين صلبين أو أكثر  
✅ تريد تغيير أعمق للـ Hardware IDs  
✅ مستخدم متقدم

❌ **إذا لديك قرص واحد فقط** → استخدم [Normal Reinstall](normal-reinstall.md)

---

## 📦 المتطلبات

- 💾 قرصين صلبين على الأقل
- 🔧 لوحة أم تدعم AMD RAID
- 💿 Windows ISO
- 📀 AMD RAID Drivers

---

## ⚙️ الخطوة 1: تفعيل RAID في BIOS

```
1. ادخل BIOS (Del أثناء الإقلاع)
2. اذهب إلى: Storage Configuration
3. اختر SATA Mode: RAID
4. احفظ وأعد التشغيل (F10)
```

---

## 🔗 الخطوة 2: إنشاء RAID Array

**أثناء إقلاع الجهاز:**

```
1. اضغط Ctrl + F عند ظهور شعار RAID
2. اختر "Create Array"
3. اختر نوع RAID:
   • RAID 0 - سرعة (بدون حماية)
   • RAID 1 - نسخة احتياطية
4. اختر الأقراص
5. أكد الإنشاء
```

> ⚠️ **RAID 0 سيحذف كل البيانات!**

---

## 💿 الخطوة 3: تثبيت Windows مع Drivers

### عند تثبيت Windows:

```
1. أقلع من USB
2. عند "Where to install"
3. اضغط "Load Driver"
4. استعرض USB للـ AMD RAID drivers
5. ثبّت Driver
6. الآن يظهر RAID array
7. ثبّت Windows عليه
```

**🔗 تحميل AMD RAID Drivers:**  
من موقع لوحتك الأم > Support > Chipset Drivers

---

## 🔢 الخطوة 4: Disk Bypass لكل قرص

**بعد التثبيت** - غيّر Serial لكل قرص:

```cmd
diskpart
list disk
select disk 0
uniqueid disk ID=A1B2C3D4
select disk 1  
uniqueid disk ID=E5F6G7H8
exit
```

> 💡 **استخدم Serials مختلفة لكل قرص!**

---

## ✅ التحقق

```cmd
diskpart
list disk
```

يجب أن ترى قرصين مع Serials جديدة ✅

---

**التالي:** [BIOS Configurations](../bios-config.md) →
